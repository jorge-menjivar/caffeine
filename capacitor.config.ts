import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eth.jorgemenjivar.caffeine',
  appName: 'Caffeine',
  webDir: './dist/mobile',
  bundledWebRuntime: false
};

export default config;
